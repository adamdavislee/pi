# 🤖 Local core changes

This fork keeps Adam's independently reviewable `CORE:` and `EXT:` stacks as sibling branches between the empty `BASE` and `PEAK` changes. —π

## 🤖 Core changes

- Quiet transcript rendering by default: thinking blocks and tool calls stay hidden from the transcript and tree without changing session history or model context; the `verbose.ts` extension can reveal them.
