# 🤖 Adam's Pi extensions

This local Pi package contains Adam's extensions. Each extension is maintained as one independently reviewable Jujutsu change in the stack from `extension-base` through `extension-peak`; extension change descriptions use the `EXT:` prefix. —π

Load the package with:

```bash
pi install /Users/adamdavislee/clones/pi-extensions/customizations
```
