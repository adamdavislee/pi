# 🤖 Adam's Pi extensions

This local Pi package contains Adam's independently reviewable `EXT:` stack, which is a sibling of the `CORE:` stack between the empty `BASE` and `PEAK` changes. —π

Load the package with:

```bash
pi install /Users/adamdavislee/clones/pi-extensions/customizations
```

The `/verbose` command requires the `CORE: default to quiet transcript rendering` change from Adam's Pi fork.
